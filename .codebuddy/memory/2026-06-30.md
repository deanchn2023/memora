# 2026-06-30 工作日志

## 日历月视图拖拽后待办项不显示修复
- **问题**：月视图中拖拽任务更改日期后，Toast 提示成功，但目标日期格子中看不到待办项。周视图正常。
- **根因**：月视图渲染42天网格（含上月底和下月初），但 `Store.getTasksByMonth(year, month)` 只返回当月1号到下月1号的任务。拖到上月底/下月初格子时，任务不在查询范围内。
- **修复文件**：`src/scripts/calendar.js` 的 `renderMonthView()` 方法
- **修复方案**：改为按整个42天网格范围获取任务（计算 startDate 和 endDate，用 `Store.getTasks()` + 范围过滤），不再依赖 `getTasksByMonth`

## 记事本分类反馈功能（轻量版）
- **设计文档**：`docs/design/feedback.md`
- **思路**：零新增 UI，复用拖拽改分类 / 点击分类标签改分类操作，在分类变更时自动记录负样本到 feedback 表
- **改动文件**：`src/scripts/app.js`
- **新增方法**：`_recordClassifyFeedback(noteId, oldCategory, newCategory)` — 查 notesCache 获取笔记内容和 analysis，调用 `electronAPI.recordFeedback` 记录 `type='classify_correction'`
- **改动点1**：拖拽改分类的 drop 回调（~1037行），`notebookUpdateNote` 成功后调用 `_recordClassifyFeedback`
- **改动点2**：`changeNoteCategory()` 方法（~14301行），点击分类标签改分类成功后调用 `_recordClassifyFeedback`
- **不需要开发的部分**：新增分类动态学习（已有 `{{#each custom_categories}}` 注入）；ADP 回答触发（知识跟随模块已支持 `intent=question` 自动触发）
- **数据流**：拖拽改分类 → recordFeedback → feedback 表 → 下次分类时作为负样本注入 Prompt → AI 分类准确率提升

## 剪贴板预分类短文本过滤
- **问题**：短文本（单字、纯数字、纯人名、纯词组、纯英文短语）被存入记事本，如"翚""893157353""孔德明""私有化""Forward Deployed Engineer"
- **根因**：`preClassify` 函数（main.js:1472）没有最小长度和短文本类型过滤
- **修复**：在 preClassify 中增加短文本过滤规则：
  1. `trimmed.length < 6` 且不含@提及/编号列表 → 拒绝（内容过短）
  2. 纯数字（如 893157353）→ 拒绝
  3. 单字（如 翚）→ 拒绝
  4. 纯英文 ≤30 字无中文无问号（如 Forward Deployed Engineer）→ 拒绝
  5. 纯汉字词组 1-4 字无标点（如 私有化、孔德明）→ 拒绝
  6. 含@提及或编号列表的短文本仍然放行（强待办信号）

## 剪贴板任务专家匹配规则扩大 + 会话保存修复
- **改动**：`main.js` `_matchExpertForTask` 的匹配规则扩大，新增"信息收集/汇总/表格生成/文档初版"等可自动处理类型，默认策略：含"收集""整理""汇总""生成"且有产出物 → can_auto_process=true
- **改动**：`main.js` fallback suggestedPrompt 追加"请直接生成初版方案/表格"
- **改动**：`prompts/clipboard_expert_match.md` 同步更新
- **会话保存修复**：`app.js` `_handleExpertMatchedTask` 去掉 setTimeout(300ms) 延迟改为 await，sendAIMessage 后强制调用 `_saveSessionMessagesForSession` 同步保存（不防抖）
- **@自己也识别为待办**：`prompts/task_recognition_v2.0.md` 新增规则：@提及对象是用户自己也视为待办（@自己+行动动词 → is_task=true）
- **图片下载404修复**：`main.js` ~11180 行，`syncApiRequest('/notes/images?limit=50')` 缺少 `method:'GET'`，默认 POST 导致 "Cannot POST /memora/sync/notes/images"。修复为显式指定 GET 方法
