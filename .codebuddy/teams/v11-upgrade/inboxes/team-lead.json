[
  {
    "id": "msg-1779980984023-lruce3",
    "from": "backend-dev",
    "to": "team-lead",
    "type": "message",
    "content": "Memora 后端搜索结果如下：\n\n## 1. `crypto` require\n**main.js 中没有 `crypto` require。** 文件头部（1-3行）只引入了：\n- L1: `electron` (app, BrowserWindow, ipcMain, clipboard, Notification, Tray, Menu, nativeImage)\n- L2: `path`\n- L3: `child_process` (exec)\n\n需要新增 `const crypto = require('crypto');`\n\n## 2. `let memoryStore;` 和 `let notebook;` 声明位置\n- **L38**: `let memoryStore;`\n- **L42**: `let notebook;`\n\n## 3. app.whenReady() 初始化代码\n**L1735-1772**:\n```js\napp.whenReady().then(() => {\n  console.log('[App] Starting 忆境 Memora...');\n  \n  // 初始化数据库层（同步）\n  db = new Database(app.getPath('userData'));\n  db.init();\n  \n  // 初始化 i18n\n  const savedLocale = db.getSettings().locale || 'zh-CN';\n  I18n.init(savedLocale);\n  \n  // 初始化记忆系统\n  memoryStore = new MemoryStore();\n  \n  // 初始化记事本系统\n  notebook = new Notebook();\n  \n  createWindow();\n  createTray();\n  startClipboardWatcher();\n  startAutoBackup();\n\n  app.on('activate', () => { ... });\n});\n```\n\n## 4. 已有 IPC handlers 的最后位置\n按文件顺序，最后的 IPC handler 是：\n- **L1937-1939**: `ipcMain.handle('i18n-t', ...)`\n\n但如果按功能区域划分：\n- i18n handlers: L1918-1939\n- 多窗口: L1907-1915\n- 数据库 IPC: L1809-1849\n- 记忆+记事本+反馈+Prompt: L1399-1717\n\n**最后一个 IPC handler 行号: L1939**\n\n## 5. record-feedback / feedback 相关 IPC handler\n**已存在！** 位于 **L1602-1619**:\n```js\nipcMain.handle('record-feedback', async (event, feedback) => {\n  feedbackStore.push({\n    ...feedback,\n    id: Date.now().toString() + '-' + Math.random().toString(36).substr(2, 9)\n  });\n  if (feedbackStore.length > 100) {\n    feedbackStore = feedbackStore.slice(-100);\n  }\n  if (feedbackStore.length % 10 === 0) {\n    optimizePrompts();\n  }\n  return { success: true };\n});\n```\n还有一个 `optimize-prompts` handler 在 L1714-1717，以及 `optimizePrompts()` 函数在 L1622-1712。\n反馈存储用的是内存变量 `let feedbackStore = [];`（L1600），不是持久化数据库。\n\n## 6. preload.js 中 electronAPI 完整结构\n```js\ncontextBridge.exposeInMainWorld('electronAPI', {\n  // 基础功能\n  estimateDuration, addToCalendar, showNotification, getClipboardText,\n  \n  // AI统计和配置\n  getAIStats, setAIDailyLimit, getAIPrompt, setAIPrompt, resetAIPrompt,\n  \n  // API配置\n  getAPIConfig, setAPIConfig, clearAPIKey,\n  \n  // ADP配置\n  getADPConfig, setADPConfig, sendADPMessage, clearADPConfig,\n  \n  // 记忆系统\n  getMemories, addMemory, updateMemory, deleteMemory, clearAllMemories,\n  getMemoryStats, getEntityGraph, searchRelatedMemories,\n  getMemoryPrompt, setMemoryPrompt, resetMemoryPrompt, extractMemory,\n  analyzeTask, analyzeClipboard, optimizeClipboardPrompt,\n  \n  // 记事本系统\n  notebookAddNote, notebookSearch, notebookGetNotes, notebookGetNote,\n  notebookUpdateNote, notebookDeleteNote, notebookDeleteNotesByCategory, notebookGetStats,\n  \n  // 反馈系统\n  recordFeedback, optimizePrompts,\n  \n  // 剪切板去重管理\n  clearClipboardHashes, getClipboardHashCount,\n  \n  // 事件监听\n  onClipboardTaskDetected, onStartPomodoro, onNewNoteAdded,\n  \n  // 窗口控制\n  minimizeWindow, maximizeWindow, closeWindow,\n  \n  // 数据库操作\n  dbGetTasks, dbSaveTasks, dbGetStats, dbCreateBackup, dbListBackups,\n  dbRestoreBackup, dbExportData, dbImportData,\n  \n  // 多窗口\n  openChildWindow,\n  \n  // i18n\n  i18nGetLocale, i18nSetLocale, i18nGetTranslations, i18nT\n});\n```\n\n总共 **47个方法** 暴露在 electronAPI 上。",
    "timestamp": "2026-05-28T15:09:44.023Z",
    "read": true
  }
]